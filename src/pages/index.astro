---
import Base from '../layouts/Base.astro';
import Hero from '../components/Hero.astro';
import ProjectCard from '../components/ProjectCard.astro';
import About from '../components/About.astro';
import ContactForm from '../components/ContactForm.astro';

export const prerender = true;
---

<Base>
  <main>
    <Hero />

    <section id="about" class="max-w-3xl mx-auto px-6 py-24">
      <About />
    </section>

    <section id="projects" class="max-w-4xl mx-auto px-6 py-24">
      <h2 class="font-display text-3xl text-accent mb-12">// projects</h2>
      <div class="flex flex-col gap-8">
        <ProjectCard
          name="habit"
          description="A productivity based app that syncs with your school account to track and organize your assignments."
          tags={['School', 'Assignments', 'Productivity', 'Calendar']}
          url="https://habit.mitools.dev"
        />
        <ProjectCard
          name="crimsontools"
          description="A research tool that organizes your sources, history projects, bibliographies and ideas."
          tags={['Research', 'History', 'Bibliography', 'Notecards']}
          url="https://crimsontools.mitools.dev"
        />
        <ProjectCard
          name="trueclips"
          description="An automated clip farm that watches popular Twitch streamers, uses pattern recognition to capture highlights, and edits them into short-form videos."
          tags={['Twitch', 'Automation', 'Pattern Recognition', 'Shorts']}
          url="https://www.youtube.com/@true_clips-k3v/shorts"
        />
      </div>
    </section>

    <section id="contact" class="max-w-3xl mx-auto px-6 py-24">
      <ContactForm />
    </section>
  </main>
</Base>
